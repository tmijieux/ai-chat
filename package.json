{
  "name": "my-ai-chat",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently --names backend,frontend --prefix-colors cyan,magenta \"cd backend && uv run python serve.py\" \"cd chat-client && npm start\"",
    "backend": "cd backend && uv run python serve.py",
    "frontend": "cd chat-client && npm start"
  },
  "devDependencies": {
    "concurrently": "^9"
  }
}
