[project]
name = "my-ai-chat-whisper"
version = "0.0.0"
description = "OpenVINO Whisper speech-to-text tooling (separate env from the backend — conflicting transformers/optimum versions)"
requires-python = ">=3.12"
dependencies = [
    "optimum[openvino]==2.1.0",
    "openvino==2026.2.0",
    "sounddevice==0.5.5",
    "soundfile==0.13.1",
    "numpy==2.4.6",
]

[tool.uv]
package = false
